  <property name="version" value="1.0.0" />
  <property name="build" value="build" />
  <property name="source" value="src" />
  <property name="lib" value="lib" />
  <property name="buildsrc" value="${build}/src" />
  <property name="classes"  value="${build}/classes" />
  <property name="masterbuild" value="${basedir}/../.." />
  <property name="masterlib" value="${masterbuild}/lib" />
  <property name="masterdist" value="${masterbuild}/dist" />
  <property name="masterbuild.build" value="${masterbuild}/build" />
  <property name="classpath" value="${classpath}:${classes}"/>

  <property name="debug" value="on"/>
  <property name="deprecation" value="false"/>
  
  <path id="xdoclet.classpath">
    <pathelement location="${masterlib}/xjavadoc-1.0.2.jar"/>
    <pathelement location="${masterlib}/xdoclet-1.2.jar"/>
    <pathelement location="${masterlib}/xdoclet-jmx-module-1.2.jar"/>
    <pathelement location="${masterlib}/xdoclet-mx4j-module-1.2.jar"/>
  </path>

  <path id="lib.classpath">
  <pathelement path="${classpath}"/>
  <fileset dir="${masterlib}">
    <include name="*.jar" />
  </fileset>
  <fileset dir="${lib}">
    <include name="*.jar" />
  </fileset>
  <pathelement location="${classes}"/>
  </path>
